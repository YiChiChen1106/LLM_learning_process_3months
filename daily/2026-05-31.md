# 2026-05-31 - Day22 Hugging Face / PEFT 训练数据流

## 今天做了什么

- 继续阅读 `projects/llm-lora-finetuning/train_lora.py`。
- 重点理解从 JSONL 到 Trainer 的训练数据流。
- 复盘 LoRA/PEFT 训练时保存的是什么。
- 今天不修改训练脚本，只记录源码里的行为和一个后续可优化点。

## 数据从哪里来

源码里的入口是：

```python
def load_jsonl(path: str) -> Dataset:
    rows = []
    with Path(path).open(encoding="utf-8") as f:
        for line in f:
            rows.append(json.loads(line))
    return Dataset.from_list(rows)
```

这里的流程是：

```text
JSONL 文件
-> 每一行 json.loads 成一个 dict
-> rows 列表
-> Hugging Face Dataset
```

所以原始样本通常长这样：

```text
instruction: 用户问题
response: 助手回答
```

## prompt 是怎么拼出来的

`format_example` 把一条样本拼成 chat 格式字符串：

```python
def format_example(example: dict[str, str]) -> str:
    return (
        "<|im_start|>user\n"
        f"{example['instruction']}<|im_end|>\n"
        "<|im_start|>assistant\n"
        f"{example['response']}<|im_end|>"
    )
```

这里还不是 token id，只是普通字符串。`instruction` 和 `response` 的内容会进入训练文本，但 `"instruction"` 和 `"response"` 这两个字段名本身不会被拼进去。

## tokenizer 做了什么

训练脚本里：

```python
encoded = tokenizer(
    text,
    truncation=True,
    max_length=cfg["max_length"],
    padding="max_length",
)
encoded["labels"] = encoded["input_ids"].copy()
```

tokenizer 会把字符串变成模型能读的编号：

```text
text -> input_ids / attention_mask
```

- `input_ids`: token 对应的整数 id。
- `attention_mask`: 真 token 通常是 1，padding token 通常是 0。
- `labels`: 用来计算 loss 的答案。

`truncation=True` 表示样本太长时截断到 `max_length`。

`padding="max_length"` 表示样本太短时补齐到 `max_length`。

## 原始列什么时候被删除

源码里真正删除 `instruction/response` 原始列的是这一行：

```python
tokenized = dataset.map(tokenize, remove_columns=dataset.column_names)
```

`dataset.column_names` 原本包含 `instruction` 和 `response`。执行完之后，训练用的数据主要剩下：

```text
input_ids
attention_mask
labels
```

`remove_unused_columns=False` 是后面 `TrainingArguments` 给 Trainer 的设置，不是删除 `instruction/response` 的主因。

## 今天看到的一个源码小坑

当前脚本是：

```python
encoded["labels"] = encoded["input_ids"].copy()
```

这会让 padding 位置的 label 也等于 pad token id。更理想的做法是根据 `attention_mask`，把 padding 位置的 label 改成 `-100`：

```python
labels = [
    token_id if mask == 1 else -100
    for token_id, mask in zip(encoded["input_ids"], encoded["attention_mask"])
]
```

这样 `CrossEntropyLoss(ignore_index=-100)` 就会跳过 padding 位置，不把它们算进 loss。

今天先不改这个逻辑，只把它作为后续优化点记下来。

## Trainer 里几个训练参数

### `gradient_accumulation_steps`

如果：

```yaml
gradient_accumulation_steps: 8
```

意思是每 8 个小 batch 累积一次梯度，再更新一次参数。有效 batch size 大致是：

```text
per_device_train_batch_size * gradient_accumulation_steps * GPU 数量
```

### `bf16`

源码里：

```python
bf16=torch.cuda.is_available()
```

意思是有 CUDA GPU 时启用 bf16；没有 CUDA GPU 时不启用。它不是 LoRA rank，也不是 QLoRA 专属配置，只是训练精度设置。

### `trainer.save_model`

PEFT/LoRA 场景里：

```python
trainer.save_model(cfg["output_dir"])
```

通常保存的更像 LoRA adapter，而不是完整 base model。它类似 toy LoRA/QLoRA 里保存：

```text
lora_A.weight
lora_B.weight
```

所以之后推理一般需要：

```text
base model + adapter
```

或者先 merge adapter，再保存 merged model。

## 今天能回答的问题

### Q1: `instruction` 和 `response` 这两个列名最后还在吗？

不在。它们会被：

```python
remove_columns=dataset.column_names
```

删掉。但它们里面的文本内容已经被 `format_example` 拼进字符串，并被 tokenizer 转成了 `input_ids`。

### Q2: 如果样本超过 `max_length` 会怎样？

会因为：

```python
truncation=True
```

被截断到 `max_length`。

### Q3: 如果样本短于 `max_length` 会怎样？

会因为：

```python
padding="max_length"
```

被补齐到 `max_length`。

### Q4: pad token 的 `attention_mask` 通常是什么？

通常是 0。真 token 通常是 1。

### Q5: 当前源码里 pad 位置的 label 是什么？

因为当前只是：

```python
labels = input_ids.copy()
```

所以 pad 位置的 label 仍然是 pad token id，不是 `-100`。

### Q6: LoRA/PEFT 训练后保存的是完整 base model 吗？

通常不是。保存的重点是 adapter。adapter checkpoint 不能单独 generate，需要搭配 base model 使用，或者先 merge。

## 今天的结论

今天可以把训练数据流记成：

```text
JSONL
-> load_jsonl
-> Hugging Face Dataset
-> format_example 拼成 chat 文本
-> tokenizer 转成 input_ids / attention_mask
-> labels = input_ids.copy()
-> map 时删除 instruction/response 原始列
-> Trainer 用 input_ids / attention_mask / labels 训练
-> save_model 保存 LoRA adapter
```

一句话总结：

```text
instruction/response 只是原始数据源；
真正喂给模型训练的是 input_ids / attention_mask / labels；
LoRA 训练主要更新和保存 adapter，不是完整 base model。
```

## 下一步

- 可以考虑把 padding 位置的 label 改成 `-100`。
- 再继续看 Trainer 如何调用模型 forward，并理解 causal LM loss 的 shift 发生在哪里。
