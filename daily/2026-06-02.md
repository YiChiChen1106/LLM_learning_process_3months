# 2026-06-02 - Day24 Assistant-only loss mask

## 今天做了什么

- 接 Day23 的 padding label mask，继续推进到 assistant-only loss mask。
- 理解为什么 SFT 里通常不希望 user prompt 参与 loss。
- 修改 `projects/llm-lora-finetuning/train_lora.py`，让 labels 只保留 assistant response 部分。
- 更新 `projects/llm-lora-finetuning/test_train_lora.py`，覆盖 prompt mask 和 padding mask。

## 从 padding mask 到 assistant-only mask

Day23 的逻辑是：

```text
padding -> -100
非 padding -> token id
```

这样可以避免 padding token 参与 loss。

Day24 再往前走一步。instruction tuning / SFT 里，输入通常是：

```text
<|im_start|>user
用户问题<|im_end|>
<|im_start|>assistant
助手回答<|im_end|>
```

如果所有非 padding token 都参与 loss，模型会同时学习复现 user prompt、模板 token 和 assistant response。

更常见的 SFT 目标是：

```text
prompt / user / template -> -100
assistant response       -> token id
padding                  -> -100
```

一句话：

```text
padding mask 是不训练补出来的 token；
assistant-only mask 是不训练用户输入，只训练助手回答。
```

## 最小实现思路

先拆出 prompt：

```python
def format_prompt(example: dict[str, str]) -> str:
    return (
        "<|im_start|>user\n"
        f"{example['instruction']}<|im_end|>\n"
        "<|im_start|>assistant\n"
    )
```

完整样本继续是：

```python
def format_example(example: dict[str, str]) -> str:
    return format_prompt(example) + f"{example['response']}<|im_end|>"
```

在 `tokenize()` 里：

```python
prompt_ids = tokenizer(
    prompt,
    truncation=True,
    max_length=cfg["max_length"],
    padding=False,
)["input_ids"]
```

这里 `prompt_ids` 不能用 `padding="max_length"`，因为我们只想知道真实 prompt 有多少 token。如果 prompt tokenize 也 padding，`prompt_len` 会被撑到 `max_length`，导致 assistant response 也被 mask 掉。

然后 full text 再 padding：

```python
encoded = tokenizer(
    text,
    truncation=True,
    max_length=cfg["max_length"],
    padding="max_length",
)
```

最后生成 labels：

```python
prompt_len = min(len(prompt_ids), len(encoded["input_ids"]))
encoded["labels"] = [
    token_id if mask == 1 and i >= prompt_len else -100
    for i, (token_id, mask) in enumerate(
        zip(encoded["input_ids"], encoded["attention_mask"])
    )
]
```

保留一个 token 的 label，需要同时满足：

```text
不是 padding
并且位置 i >= prompt_len
```

## 为什么是 `i >= prompt_len`

如果：

```text
prompt_len = 5
```

prompt token 的下标是：

```text
0, 1, 2, 3, 4
```

第一个 assistant token 的下标就是：

```text
5
```

所以条件必须是：

```python
i >= prompt_len
```

如果写成 `i > prompt_len`，第一个 assistant token 会被误 mask 掉。

## 和 Causal LM shift 的关系

假设：

```text
input_ids:
[prompt1, prompt2, assistant1, assistant2, eos, pad]

labels:
[-100,   -100,   assistant1, assistant2, eos, -100]
```

Causal LM 内部会做：

```python
shift_logits = logits[:, :-1, :]
shift_labels = labels[:, 1:]
```

shift 后：

```text
shift_labels:
[-100, assistant1, assistant2, eos, -100]
```

实际效果是：

```text
位置 0 的输出 -> 忽略
位置 1 的输出 -> 预测 assistant1
位置 2 的输出 -> 预测 assistant2
位置 3 的输出 -> 预测 eos
位置 4 的输出 -> 忽略
```

所以保留第一个 assistant token 很重要：shift 后，它会成为 prompt 末尾位置要预测的答案。

## 今天的代码修改

修改 `projects/llm-lora-finetuning/train_lora.py`：

- 新增 `format_prompt()`。
- 让 `format_example()` 复用 `format_prompt()`。
- `tokenize()` 先 tokenize prompt 得到 `prompt_len`。
- labels 只保留非 padding 且位于 assistant response 区域的 token。

更新 `projects/llm-lora-finetuning/test_train_lora.py`：

- 旧测试改为覆盖 prompt-only + padding 时 labels 全为 `-100`。
- 新增测试覆盖 prompt + assistant response + padding：

```text
input_ids      = [10, 11, 12, 20, 21, 0]
attention_mask = [1,  1,  1,  1,  1, 0]
labels         = [-100, -100, -100, 20, 21, -100]
```

## 验证

今天跑过：

```text
python -m pytest projects\llm-lora-finetuning\test_train_lora.py
```

结果：

```text
2 passed
```

还跑过：

```text
python -m pytest projects\toy-qlora-from-scratch projects\toy-lora-from-scratch
```

结果：

```text
14 passed
```

## 今天的结论

```text
SFT 的 labels mask 不只是去掉 padding，还要去掉 prompt；
assistant response 的第一个 token 要保留；
因为 shift 后，它会让 prompt 末尾学会预测回答开头。
```

## 下一步

- 找 Hugging Face Causal LM shift loss 的真实源码位置。
- 学习只对 assistant message 算 loss 的更通用数据 collator 写法。
- 后续可以把配置项扩展成是否启用 assistant-only loss mask。
